%% -*-: erlang -*-
{"e4.2.1",
  [
    {"e4.2.0", [
      {restart_application, emqx_exproto},
      {apply, {emqx_plugins, load, []}},
      {apply, {emqx_modules, refresh_module, [exproto]}}
    ]}
  ],
  [
    {"e4.2.0", [
      {restart_application, emqx_exproto},
      {apply, {emqx_plugins, load, []}}
    ]}
  ]
}.
