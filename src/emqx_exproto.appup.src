%% -*-: erlang -*-
{"4.2.1",
  [
    {"e4.2.0", [
      {remove_application, grpcbox},
      {add_application, grpc},
      {restart_application, emqx_exproto}
    ]}
  ],
  [
    {"e4.2.0", [
      {remove_application, grpc},
      {add_application, grpcbox},
      {restart_application, emqx_exproto}
    ]}
  ]
}.
